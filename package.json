{
  "name": "crypto-live-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run build --prefix client\"",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix client",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
